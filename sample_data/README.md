# Sample data

This folder contains example annotation JSON for local or web-upload testing.

- `sample.annotations.json`: valid annotation payload matching the annotation schema
- `sample.pdf`: not included; in local path mode, use any matching PDF path, and in web mode upload your own PDF together with the sample annotation JSON
